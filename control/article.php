<?phpinclude_once('../models/news.php');include_once('../models/startup.php');header('Content-type: text/html; charset=utf-8');// Массив со статьёй$article = articles_get($_GET['id']);// Внутренний шаблон.$content = view_include(	'../views/article.php',	array('article' => $article));// Внешний шаблон.$page = view_include(	'../views/index.php',	array('title' => 'Просмотр статьи', 'content' => $content));// Вывод.echo $page;